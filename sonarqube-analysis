node{
  stage('SCM Checkout'){
    git 'https://github.com/Janeth16mtz/DOTT-1.git/'
  }
  stage('Compile-Package'){
   //Get maven home path
   def mvnHome = tool 'M3'
   sh "${mvnHome}/bin/mvn -B verify"
  }
}
